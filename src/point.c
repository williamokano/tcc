#include "../headers/point.h"

Point2D point2d(int x, int y) {
    Point2D point;
    point.x = x;
    point.y = y;

    return point;
}

Point3D point3d(int x, int y, int z) {
    Point3D point;
    point.x = x;
    point.y = y;
    point.z = z;

    return point;
}
